services:
  - type: web
    name: phishguard
    env: node
    plan: free
    region: oregon
    buildCommand: bash build.sh
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
